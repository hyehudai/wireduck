# name: test/sql/wireduck.test
# description: test wireduck extension
# group: [wireduck]

# Before we load the extension, this will fail
statement error
SELECT wireduck('Sam');
----
Catalog Error: Scalar Function with name wireduck does not exist!

# Require statement will ensure this test is run with this extension loaded
require wireduck

# Confirm the extension works
query I
SELECT wireduck('Sam');
----
Wireduck Sam ^^^

query I
SELECT wireduck_openssl_version('Michael') ILIKE 'Wireduck Michael, my linked OpenSSL version is OpenSSL%';
----
true

